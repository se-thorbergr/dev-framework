; dev-framework se-config.ini template
; Checked into git; mirrors ProjectName.mdk.ini (shared defaults only).

[Paths]
; Optional helper overrides to aid discovery when binarypath is auto.
; steam_path = C:/Program Files/Steam
; game_path = C:/Program Files/Steam/steamapps/common/SpaceEngineers

[mdk]
; Project-level defaults (keep aligned with MDK² configuration guide).
type=programmableblock
trace=off
minify=none
ignores=obj/**/*,MDK/**/*,**/*.debug.cs
; donotclean =

; Local-only keys (output/binarypath) belong in se-config.local.ini, not here.
